import { Agent, Message, NewsroomAPI } from '../types/api';
const baseURL = process.env.EXPO_PUBLIC_API_BASE_URL ?? 'http://localhost:8080';
async function request<T>(path: string, init?: RequestInit): Promise<T> { const response = await fetch(`${baseURL}${path}`, { ...init, headers:{'Content-Type':'application/json', ...(init?.headers ?? {})} }); const body = await response.json(); if (!response.ok) throw new Error(body?.error?.message ?? 'Request failed'); return body; }
export const httpAPI: NewsroomAPI = { async listAgents() { return (await request<{agents:Agent[]}>('/api/v1/agents')).agents; }, async listMessages(agentId) { return (await request<{messages:Message[]}>(`/api/v1/agents/${agentId}/messages`)).messages; }, async startRun() { throw new Error('Starting research is not implemented yet.'); }, async postMessage() { throw new Error('Posting messages is not implemented yet.'); }, async updateDraft() { throw new Error('Updating drafts is not implemented yet.'); } };
