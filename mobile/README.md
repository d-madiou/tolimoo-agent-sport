# Signal Mobile

This is the Expo companion scaffold for Signal. Run `npm install` in this folder, then `npx expo start`.

The mobile screen uses the same story-card contract as the web shell: category, headline, source list, timestamp, verification state, and saved state. Connect both clients to the shared API when the ingestion pipeline is available.
